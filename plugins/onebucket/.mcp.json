{
  "mcpServers": {
    "onebucket": {
      "type": "http",
      "url": "${user_config.endpoint}"
    },
    "onebucket-events": {
      "type": "http",
      "url": "${user_config.events_endpoint}"
    }
  }
}
